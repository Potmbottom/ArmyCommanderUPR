namespace ArmyCommander
{
    public enum Team { Allied, Enemy }

    public enum TroopState { Idle, Move, Attack, Dead }

    public enum AIBehaviour { Home, Aggressive }

    public enum ProjectileState { Active, Destroyed }

    public enum TroopType { Empty, Soldier, Veteran, Master }

    public enum ArmyLevel { Level1 = 1, Level2 = 2, Level3 = 3 }

    public enum ResourceType { Gold, Silver }
}
